# Copy to .env.production and point at your hosted API before running `npm run build`.
# Leave unset for local development (defaults to http://localhost:3000).
VITE_API_BASE=https://api.royaldropsafaris.com
